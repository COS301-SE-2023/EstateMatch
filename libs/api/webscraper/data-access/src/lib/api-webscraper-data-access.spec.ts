import { apiWebscraperDataAccess } from './api-webscraper-data-access';

describe('apiWebscraperDataAccess', () => {
  it('should work', () => {
    expect(apiWebscraperDataAccess()).toEqual('api-webscraper-data-access');
  });
});
