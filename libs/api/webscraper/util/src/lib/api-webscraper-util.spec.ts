import { apiWebscraperUtil } from './api-webscraper-util';

describe('apiWebscraperUtil', () => {
  it('should work', () => {
    expect(apiWebscraperUtil()).toEqual('api-webscraper-util');
  });
});
