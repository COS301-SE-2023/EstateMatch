import { apiUsersDataAccess } from './api-users-data-access';

describe('apiUsersDataAccess', () => {
  it('should work', () => {
    expect(apiUsersDataAccess()).toEqual('api-users-data-access');
  });
});
