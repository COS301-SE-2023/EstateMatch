import { appHeaderFooterDataAccess } from './app-header-footer-data-access';

describe('appHeaderFooterDataAccess', () => {
  it('should work', () => {
    expect(appHeaderFooterDataAccess()).toEqual(
      'app-header-footer-data-access'
    );
  });
});
