import { appHeaderFooterUtilUnitTestRunnerJest } from './app-header-footer-util--unit-test-runner=jest';

describe('appHeaderFooterUtilUnitTestRunnerJest', () => {
  it('should work', () => {
    expect(appHeaderFooterUtilUnitTestRunnerJest()).toEqual(
      'app-header-footer-util--unit-test-runner=jest'
    );
  });
});
