import { appSearchFilterFeature } from './app-search-filter-feature';

describe('appSearchFilterFeature', () => {
  it('should work', () => {
    expect(appSearchFilterFeature()).toEqual('app-search-filter-feature');
  });
});
