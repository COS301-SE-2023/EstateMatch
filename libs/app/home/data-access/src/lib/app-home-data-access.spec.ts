import { appHomeDataAccess } from './app-home-data-access';

describe('appHomeDataAccess', () => {
  it('should work', () => {
    expect(appHomeDataAccess()).toEqual('app-home-data-access');
  });
});
