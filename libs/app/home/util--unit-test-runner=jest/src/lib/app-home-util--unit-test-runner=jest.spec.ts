import { appHomeUtilUnitTestRunnerJest } from './app-home-util--unit-test-runner=jest';

describe('appHomeUtilUnitTestRunnerJest', () => {
  it('should work', () => {
    expect(appHomeUtilUnitTestRunnerJest()).toEqual(
      'app-home-util--unit-test-runner=jest'
    );
  });
});
