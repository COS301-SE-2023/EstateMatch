import { appMoreInfoDataAccess } from './app-more-info-data-access';

describe('appMoreInfoDataAccess', () => {
  it('should work', () => {
    expect(appMoreInfoDataAccess()).toEqual('app-more-info-data-access');
  });
});
